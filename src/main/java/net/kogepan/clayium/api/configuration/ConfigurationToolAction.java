package net.kogepan.clayium.api.configuration;

/** Operations that a configuration tool can request from a configurable machine. */
public enum ConfigurationToolAction {
    INSERTION,
    EXTRACTION,
    PIPING,
    ROTATION,
    FILTER_REMOVER
}
